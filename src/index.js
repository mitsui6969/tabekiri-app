import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Appコンポーネントを読み込む
import "leaflet/dist/leaflet.css"; // LeafletのCSSを読み込む

ReactDOM.render(<App />, document.getElementById("root"));
